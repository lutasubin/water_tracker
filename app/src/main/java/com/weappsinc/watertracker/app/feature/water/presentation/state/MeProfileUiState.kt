package com.weappsinc.watertracker.app.feature.water.presentation.state

/** UI màn Me: tổng ml đã uống + streak (số ngày) + menu. */
data class MeProfileUiState(
    val totalDrinkingMl: Int,
    val streakDays: Int,
)
